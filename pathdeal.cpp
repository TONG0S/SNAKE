
#include <iostream>
#include "windows.h"
#include "conio.h"
#include "data.h"
#include <time.h>
#include "fileInfo.h"
using std::cout;
using std::endl;
#if 0
void other() {

		if(GetAsyncKeyState(VK_UP)) {
			button_Snake = UP;
		}
		else if (GetAsyncKeyState(VK_UP)) {
			button_Snake = DOWN;
		}
		else if (GetAsyncKeyState(VK_UP)) {
			button_Snake = LEFT;
		}
		else if (GetAsyncKeyState(VK_UP)) {
			button_Snake = RIGHT;
		}
	
}
#endif

